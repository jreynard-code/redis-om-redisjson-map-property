openapi: 3.0.3

info:
  title: ""
  version: ""

tags:
  - name: working
    description: "Working endpoints"

paths:
  /test-serialization-map:
    post:
      tags:
        - working
      operationId: testSerializationMap
      summary: Test map serialization
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/TestSerializationMapRequest'
      responses:
        "200":
          description: Serialization result

components:
  schemas:
    TestSerializationMapRequest:
      type: object
      required:
        - value
      properties:
        value:
          type: string
        additionalData:
          type: object
          additionalProperties: true
          description: Free form additional data
